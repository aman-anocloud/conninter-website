/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'placehold.co', 'via.placeholder.com', 'images.unsplash.com'],
    },
};

module.exports = nextConfig;
